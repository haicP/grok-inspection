## 更新内容

- 新增「巡检当前分类」：点击上方分类卡片后，可只重新探测该分类下的账号，其他结果保留。
- `/start` 支持 `classifications` 参数；特殊值 `other` 匹配非主分类（探测异常/模型不可用/未知等）。
- 分类巡检就地更新匹配账号，不会清空已有巡检列表。
- 进度文案区分完整 / 增量 / 分类三种模式。

## 验证

- `go test ./... -count=1`

**完整变更记录**：https://github.com/ywddd/grok-inspection/compare/v0.1.6...v0.1.7
